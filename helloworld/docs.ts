// deno doc --html --name="doc" docs.ts

/**
 * @description This is a simple function that adds two numbers
 * @param a
 * @param b
 * @returns The multiplication of a and b
 */
export function multiply(a: number, b: number): number {
  return a * b
}
